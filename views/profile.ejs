<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Professional Profile Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #2c003e;
      color: white;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .profile-container {
      width: 600px;
      background-color: #004b63;
      border-radius: 16px;
      padding: 40px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      text-align: center;
    }

    .avatar {
      margin-bottom: 30px;
    }

    .avatar img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      background-color: yellow; /* Placeholder for avatar background */
      display: block;
      margin: 0 auto;
    }

    .profile-info {
      margin-bottom: 20px;
      text-align: left;
      font-size: 18px;
      line-height: 1.6;
    }

    .profile-info p {
      margin: 12px 0;
    }

    .profile-info p strong {
      color: #ffa500;
    }

    .back-button {
      margin-top: 30px;
      padding: 15px 30px;
      font-size: 18px;
      font-weight: bold;
      color: #004b63;
      background-color: #ffa500;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .back-button:hover {
      background-color: #e69500;
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <div class="profile-container">
    <div class="avatar">
      <img src="avatar-placeholder.png" alt="Profile Avatar">
    </div>
    <div class="profile-info">
      <p><strong>Username:</strong> <%= username %></p>
      <p><strong>Email:</strong> <%= email %></p>
      <p><strong>Name:</strong> <%= full_name %></p>
      <p><strong>College:</strong> <%= college_name %></p>
    </div>
    <button class="back-button" onclick="window.location.href='/info'">Back to Home</button>
  </div>
</body>
</html>